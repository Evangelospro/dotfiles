[managers.system]
items = [
    "nvidia-dkms", # or nvidia-open-dkms (if lucky)
    "aur/libva-nvidia-driver-git",
    "nvidia-utils",
    "nvidia-settings",
    "cuda",
    "cudnn",
    "cuda-tools",
]
