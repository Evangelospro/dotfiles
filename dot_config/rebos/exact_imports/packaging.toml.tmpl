[managers.system]
items = [
    "rebos-git",
    {{- if eq .osID "linux-arch" }}
    "pacman-static",
    "pacman-contrib",
    "rate-mirrors-bin",
    "downgrade",
    # hooks
    "pacman-cleanup-hook",
    "reboot-arch-btw",
    "kernel-modules-hook",
    {{- end }}
]

[managers.systemd-system]
items = [
    "paccache.timer",
    "linux-modules-cleanup"
]
