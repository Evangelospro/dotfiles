# systemd-system

sync = "sudo systemctl daemon-reload"
add = "sudo systemctl enable --now #:?"
remove = "if [[ $(systemctl is-enabled #:? 2>/dev/null) == enabled ]]; then sudo systemctl disable #:?; fi"

plural_name = "systemd-system"
hook_name = "systemd-system"

[config]
many_args = false
