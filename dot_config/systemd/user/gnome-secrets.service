[Unit]
Description=Gnome secrets
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/gnome-keyring-daemon --start --components=secrets

[Install]
WantedBy=graphical-session.target
